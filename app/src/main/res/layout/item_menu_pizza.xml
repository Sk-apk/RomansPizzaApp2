<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/cardMenuItem"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="2dp"
    app:strokeWidth="0dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp">

        <!-- Pizza Icon -->
        <TextView
            android:id="@+id/tvPizzaIcon"
            android:layout_width="60dp"
            android:layout_height="60dp"
            android:text="🍕"
            android:textSize="48sp"
            android:gravity="center"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent" />

        <!-- Category Badge -->
        <TextView
            android:id="@+id/tvPizzaCategory"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Classic"
            android:textSize="10sp"
            android:textColor="@color/background_light"
            android:background="@color/pizza_blue"
            android:paddingStart="8dp"
            android:paddingEnd="8dp"
            android:paddingTop="2dp"
            android:paddingBottom="2dp"
            android:layout_marginStart="8dp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toEndOf="@id/tvPizzaIcon" />

        <!-- Pizza Name -->
        <TextView
            android:id="@+id/tvPizzaName"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="Margherita"
            android:textSize="18sp"
            android:textStyle="bold"
            android:textColor="@color/text_primary"
            android:layout_marginStart="8dp"
            android:layout_marginTop="4dp"
            app:layout_constraintTop_toBottomOf="@id/tvPizzaCategory"
            app:layout_constraintStart_toEndOf="@id/tvPizzaIcon"
            app:layout_constraintEnd_toEndOf="parent" />

        <!-- Pizza Description -->
        <TextView
            android:id="@+id/tvPizzaDescription"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="Classic tomato, mozzarella &amp; fresh basil"
            android:textSize="13sp"
            android:textColor="@color/text_secondary"
            android:maxLines="2"
            android:ellipsize="end"
            android:layout_marginStart="8dp"
            android:layout_marginTop="4dp"
            app:layout_constraintTop_toBottomOf="@id/tvPizzaName"
            app:layout_constraintStart_toEndOf="@id/tvPizzaIcon"
            app:layout_constraintEnd_toEndOf="parent" />

        <!-- Price & Order Button -->
        <TextView
            android:id="@+id/tvPizzaPrice"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="From R 75.50"
            android:textSize="16sp"
            android:textStyle="bold"
            android:textColor="@color/pizza_red"
            android:layout_marginTop="12dp"
            app:layout_constraintTop_toBottomOf="@id/tvPizzaDescription"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintBottom_toBottomOf="parent" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnOrder"
            android:layout_width="wrap_content"
            android:layout_height="40dp"
            android:text="Customize"
            android:textSize="12sp"
            style="@style/Widget.Material3.Button.TonalButton"
            app:cornerRadius="20dp"
            android:layout_marginTop="8dp"
            app:layout_constraintTop_toBottomOf="@id/tvPizzaDescription"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toBottomOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</com.google.android.material.card.MaterialCardView>