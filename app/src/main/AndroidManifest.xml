<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.romanspizza">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">


        <activity
            android:name=".ui.menu.MenuActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustPan" />
        <!-- Pizza Detail Activity -->
        <activity
            android:name=".ui.menu.PizzaDetailActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />
        <!-- Cart Activity -->
        <activity
            android:name=".ui.cart.CartActivity"
            android:exported="false" />


        

        <!-- Splash Activity (Launcher) -->
        <activity
            android:name="com.example.romanspizza.ui.auth.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Login Activity -->
        <activity
            android:name="com.example.romanspizza.ui.auth.LoginActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Register Activity -->
        <activity
            android:name="com.example.romanspizza.ui.auth.RegisterActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Main Activity -->
        <activity
            android:name="com.example.romanspizza.ui.main.MainActivity"
            android:exported="false" />

    </application>

</manifest>